"""The 10 specialist + 1 manager agents that compose AgencyOS.

Each module exposes a `run(state: AgencyState) -> AgencyState` async function
that conforms to the reasoning-before-action contract described in ARCHITECTURE.md §5.
"""
