Identify the material risks to this project. Ground every risk in the requirements, plan, and
tasks below, and use the external benchmark data to judge whether the timeline and budget are
realistic for the scope.

For each risk provide:
- title: short risk name
- description: what the risk is and why it matters, in 1-2 sentences
- severity: one of "high", "medium", "low"
- mitigation: a concrete action to reduce the risk

Focus on: unrealistic deadlines, budget-vs-scope mismatch, unclear or under-specified scope,
dependency bottlenecks, and resourcing/capacity constraints. Report only genuine risks — do not
invent risks to fill space.

External benchmark data (from web search):
{{ web_context }}

Requirements (JSON):
{{ requirements_json }}

Plan (JSON):
{{ plan_json }}

Tasks (JSON):
{{ tasks_json }}
